spring:
  datasource:
    initialization-mode: always
    data: classpath:data.sql

  jpa:
    defer-datasource-initialization: true
    generate-ddl: true
    hibernate:
      show_sql: true
      ddl-auto: "create"

